'use strict';

describe('myApp.courseView module', function() {

  beforeEach(module('myApp.courseView'));

  describe('courseView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var courseViewCtrl = $controller('courseViewCtrl');
      expect(courseViewCtrl).toBeDefined();
    }));

  });
});
